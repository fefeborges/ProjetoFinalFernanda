@model IEnumerable<ProjetoFinalFernanda.Models.TipoColaborador>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo de Colaborador</h1>

<p>
    <a class="link" asp-action="Create">Criar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoColaboradorNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoColaboradorNome)
            </td>
            <td>
                <a class="link" asp-action="Edit" asp-route-id="@item.Id">Editar</a> 
                <a class="link" asp-action="Details" asp-route-id="@item.Id">Detalhes</a> 
                <a class="link" asp-action="Delete" asp-route-id="@item.Id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
